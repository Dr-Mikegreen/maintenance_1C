Не забываем ограничивать права на конфиг с паролями

PostgreSQL:
  STATISTICS → ANALYZE
  INDEXES    → REINDEX
  INTEGRITY  → доступные проверки PostgreSQL VACUUM/VACUUM FULL

MSSQL:
  STATISTICS → UPDATE STATISTICS
  INDEXES    → REORGANIZE / REBUILD
  INTEGRITY  → DBCC CHECKDB